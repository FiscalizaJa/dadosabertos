const schema = {
    $id: "senator_params",
    type: "object",
    properties: {
        id: {
            type: "number",
            description: "ID do Senador"
        }
    }
}

export default schema