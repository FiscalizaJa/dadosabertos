const schema = {
    $id: "parties_list_data",
    type: "array",
    items: {
        type: "string"
    },
    description: "Lista com todos os partidos encontrados no banco de dados."
}

export default schema